flyway:
  baseline-on-migrate: true
  
spring:
  datasource:
    url: jdbc:mysql://localhost:13306/farztev_backend_it
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver

